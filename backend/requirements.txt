--extra-index-url https://download.pytorch.org/whl/cu121

# Dependencias del backend — versiones fijadas del entorno de desarrollo activo.
# Para actualizar: pip install -U <paquete> y actualiza la versión aquí manualmente.
fastapi==0.128.7
uvicorn==0.40.0
python-multipart==0.0.22
openai-whisper==20250625
google-genai==1.62.0
websockets==15.0.1

# Soporte GPU (NVIDIA CUDA 12.1)
torch
torchvision
torchaudio

# Dependencia de pytest para tests de integración (no se usa en producción)
httpx>=0.28.1
pytest==8.3.3
